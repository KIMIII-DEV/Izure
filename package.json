{
  "name": "izure",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Izuré — public layer (portfolio) and private layer (dashboard), served by a Cloudflare Worker.",
  "scripts": {
    "graph": "node scripts/build-graph.mjs",
    "dev": "npm run lern && npm run graph && vite --host",
    "build": "npm run lern && npm run graph && vite build",
    "preview": "vite preview",
    "cf:dev": "npm run build && wrangler dev --var DEV_BYPASS:1",
    "cf:deploy": "npm run build && wrangler deploy",
    "lern": "node scripts/build-lernfelder.mjs",
    "test:learn": "node scripts/test-learn.mjs",
    "test:pruefung": "node scripts/test-pruefung.mjs",
    "test": "npm run test:learn && npm run test:pruefung"
  },
  "devDependencies": {
    "vite": "^7.1.7",
    "wrangler": "^4.111.0"
  }
}
